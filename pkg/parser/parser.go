package main

import "mime/multipart"

// Parse file to retrieve player information (message.txt has info on how python does this)

func ParseCompressedSave(save multipart.File) {
	return
}

func ParseUncompressedSave(save multipart.File) {
	return
}
